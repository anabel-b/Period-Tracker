<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:background="@drawable/img_1"
    android:padding="@dimen/heading_text_size"
    >


    <TextView
        android:id="@+id/home_welcome"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="welcome back!"
        android:textColor="#4527A0"
        android:textStyle="bold"
        android:textSize="@dimen/heading_text_size"
        android:layout_centerHorizontal="true"
        android:paddingBottom="200dp"
        />

    <Button
        android:id="@+id/period_today_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
       android:layout_below="@+id/home_welcome"
        android:layout_gravity="bottom"
        android:text="My Period Start Today!"
        android:onClick="PeriodStarts"
        android:layout_marginTop="8dp"
        android:layout_centerHorizontal="true"

        />
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/period_today_button"
        android:text="No Period Today"
        android:layout_gravity="top"
        android:onClick="noPeriod"
        android:layout_centerHorizontal="true"
        />

</RelativeLayout>